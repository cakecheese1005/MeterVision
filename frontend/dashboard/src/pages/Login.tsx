import {FormEvent,useState} from 'react'; import {useLocation,useNavigate} from 'react-router-dom'; import {useAuth} from '../hooks/useAuth'; import {extractErrorMessage} from '../api/client';
export function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');const {login,isAuthenticated}=useAuth();const nav=useNavigate();const loc=useLocation();if(isAuthenticated){nav('/',{replace:true});return null}const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await login({email,password});const from=(loc.state as {from?:{pathname:string}}|null)?.from?.pathname||'/';nav(from,{replace:true})}catch(err){setError(extractErrorMessage(err,'Invalid email or password.'))}finally{setBusy(false)}};return <div className="min-h-screen bg-slate-950 px-4 py-10 flex items-center justify-center"><div className="grid w-full max-w-5xl overflow-hidden rounded-3xl bg-white shadow-2xl md:grid-cols-2"><div className="hidden bg-ink-950 p-10 text-white md:block"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-volt-400 text-ink-950">⚡</div><div><p className="font-bold">MeterVision</p><p className="text-xs text-slate-400">PSPCL Admin</p></div></div><div className="mt-24"><p className="text-sm font-semibold text-brand-300">UTILITY OPERATIONS</p><h1 className="mt-3 text-4xl font-bold leading-tight">Smart meter reading & verification.</h1><p className="mt-5 text-sm leading-6 text-slate-400">Monitor field readings, AI verification, anomalies, LCR cases and offline synchronization from one operational dashboard.</p></div></div><div className="p-8 sm:p-10"><h2 className="text-2xl font-bold">Sign in</h2><p className="mt-1 text-sm text-slate-500">Use your PSPCL dashboard credentials.</p><form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-medium">Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2.5" placeholder="name@pspcl.in"/></label><label className="block text-sm font-medium">Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2.5" placeholder="••••••••"/></label>{error&&<div className="rounded-lg border border-rose-200 bg-rose-50 px-3 py-2 text-sm text-rose-700">{error}</div>}<button disabled={busy} className="w-full rounded-lg bg-brand-600 px-4 py-3 text-sm font-semibold text-white hover:bg-brand-700 disabled:opacity-60">{busy?'Signing in…':'Sign in'}</button></form></div></div></div>}
