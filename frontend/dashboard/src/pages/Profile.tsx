import {useEffect,useState} from 'react'; import {useAuth} from '../hooks/useAuth'; import {authApiDummy} from '../utils/profileApi'; export function Profile(){const {profile,refreshProfile}=useAuth();const [name,setName]=useState(profile?.name||'');const [phone,setPhone]=useState(profile?.phone_number||'');const [msg,setMsg]=useState('');useEffect(()=>{setName(profile?.name||'');setPhone(profile?.phone_number||'')},[profile]);const save=async()=>{try{await authApiDummy.update({name,phone_number:phone});await refreshProfile();setMsg('Profile updated.')}catch{setMsg('Unable to update profile.')}};return <div className="max-w-xl space-y-5"><div><h2 className="text-2xl font-bold">Profile</h2><p className="text-sm text-slate-500">Your authenticated backend profile.</p></div><div className="rounded-xl border bg-white p-6 shadow-sm space-y-4"><label className="block text-sm font-medium">Name<input value={name} onChange={e=>setName(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="block text-sm font-medium">Email<input value={profile?.email||''} disabled className="mt-1 w-full rounded-lg border bg-slate-50 px-3 py-2"/></label><label className="block text-sm font-medium">Phone<input value={phone} onChange={e=>setPhone(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><div className="text-sm text-slate-500">Role: <span className="font-semibold capitalize">{profile?.role}</span></div><button onClick={save} className="rounded-lg bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Save changes</button>{msg&&<p className="text-sm text-emerald-600">{msg}</p>}</div></div>}
