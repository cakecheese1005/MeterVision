import {useState} from 'react'; import {getImage} from '../api/images'; import type {Image} from '../types/api';
export function MeterImages(){const [id,setId]=useState('');const [image,setImage]=useState<Image|null>(null);const [loading,setLoading]=useState(false);const [error,setError]=useState('');const search=async()=>{if(!id.trim())return;setLoading(true);setError('');try{setImage(await getImage(id.trim()))}catch{setImage(null);setError('Image not found or unavailable.')}finally{setLoading(false)}};return <div className="space-y-5"><div><h2 className="text-2xl font-bold">Image Verification</h2><p className="text-sm text-slate-500">Inspect a meter image returned by the backend.</p></div><div className="rounded-xl border bg-white p-5 shadow-sm"><div className="flex gap-2"><input value={id} onChange={e=>setId(e.target.value)} onKeyDown={e=>e.key==='Enter'&&search()} placeholder="Enter image ID" className="w-full rounded-lg border px-3 py-2 text-sm"/><button onClick={search} className="rounded-lg bg-brand-600 px-5 py-2 text-sm font-semibold text-white">Load</button></div>{error&&<p className="mt-3 text-sm text-rose-600">{error}</p>}{loading&&<p className="mt-4 text-sm text-slate-400">Loading…</p>}{image&&<div className="mt-5 grid gap-5 lg:grid-cols-2"><div className="rounded-xl bg-slate-100 p-3"><img src={image.image_url} alt="Meter verification" className="max-h-[520px] w-full rounded-lg object-contain"/></div><div className="space-y-4"><div><p className="text-xs text-slate-400">Reading ID</p><p className="font-medium">{image.reading_id}</p></div><div><p className="text-xs text-slate-400">Quality</p><p className="font-medium capitalize">{image.quality}</p></div><div><p className="text-xs text-slate-400">Classification</p><p className="font-medium">{image.classification}</p></div><div><p className="text-xs text-slate-400">Blur score</p><p className="font-medium">{image.blur_score??'—'}</p></div><div><p className="text-xs text-slate-400">Uploaded</p><p className="font-medium">{new Date(image.uploaded_at).toLocaleString('en-IN')}</p></div></div></div>}</div></div>}
