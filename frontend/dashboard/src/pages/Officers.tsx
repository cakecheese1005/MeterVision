import {useEffect,useState} from 'react'; import {listUsers} from '../api/users'; import type {User} from '../types/api'; import {Spinner} from '../components/Spinner';
export function Officers(){const [rows,setRows]=useState<User[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{listUsers().then(setRows).finally(()=>setLoading(false))},[]);return <div className="space-y-5"><div><h2 className="text-2xl font-bold">Users & Officers</h2><p className="text-sm text-slate-500">Users available to the backend dashboard.</p></div><div className="overflow-x-auto rounded-xl border bg-white shadow-sm"><table className="min-w-[650px] w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Name</th><th className="px-4 py-3">Email</th><th className="px-4 py-3">Role</th></tr></thead><tbody className="divide-y">{loading?<tr><td colSpan={3} className="p-10 text-center"><Spinner className="mx-auto h-6 w-6"/></td></tr>:rows.map(u=><tr key={u.id}><td className="px-4 py-3 font-medium">{u.name}</td><td className="px-4 py-3">{u.email}</td><td className="px-4 py-3 capitalize">{u.role}</td></tr>)}</tbody></table></div></div>}
