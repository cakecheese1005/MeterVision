import {useEffect,useState} from 'react'; import {listConsumers,searchConsumers} from '../api/consumers'; import type {Consumer} from '../types/api'; import {Spinner} from '../components/Spinner';
export function Consumers(){const [rows,setRows]=useState<Consumer[]>([]);const [loading,setLoading]=useState(true);const [q,setQ]=useState('');const load=async()=>{setLoading(true);try{setRows(q.trim()?await searchConsumers(/^\d+$/.test(q.trim())?{consumer_number:q.trim()}:{consumer_name:q.trim()}):await listConsumers())}finally{setLoading(false)}};useEffect(()=>{load()},[]);return <div className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-2xl font-bold">Consumers</h2><p className="text-sm text-slate-500">PSPCL consumer and meter directory.</p></div><div className="flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')load()}} placeholder="Search consumer / account / name" className="w-72 rounded-lg border px-3 py-2 text-sm"/><button onClick={load} className="rounded-lg bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Search</button></div></div><div className="overflow-x-auto rounded-xl border bg-white shadow-sm"><table className="min-w-[1000px] w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr>{['Consumer','Account','Name','Meter','Type','Subdivision','Feeder','Cycle'].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{loading?<tr><td colSpan={8} className="p-10 text-center"><Spinner className="mx-auto h-6 w-6"/></td></tr>:rows.map(c=><tr key={c.id}><td className="px-4 py-3 font-medium">{c.consumer_number}</td><td className="px-4 py-3">{c.account_number}</td><td className="px-4 py-3">{c.consumer_name}</td><td className="px-4 py-3">{c.meter_number}</td><td className="px-4 py-3 capitalize">{c.meter_type}</td><td className="px-4 py-3">{c.subdivision||'—'}</td><td className="px-4 py-3">{c.feeder||'—'}</td><td className="px-4 py-3">{c.cycle||'—'}</td></tr>)}</tbody></table></div></div>}
