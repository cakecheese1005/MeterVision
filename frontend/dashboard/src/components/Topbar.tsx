import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {useAuth} from '../hooks/useAuth'; import {LogOutIcon,UserIcon} from './icons'
export function Topbar({pathname}:{pathname:string}){const {user,profile,logout}=useAuth();const nav=useNavigate();const [open,setOpen]=useState(false);const title=pathname.startsWith('/readings/')?'Reading Details':({'/':'Dashboard','/readings':'Meter Readings','/images':'Image Verification','/sync':'Sync Queue','/anomalies':'Anomalies','/lcr':'LCR Cases','/consumers':'Consumers','/officers':'Officers','/notifications':'Notifications','/profile':'Profile'} as Record<string,string>)[pathname]||'MeterVision';return <header className="flex h-16 shrink-0 items-center justify-between border-b border-slate-200 bg-white px-6"><div><h1 className="text-lg font-semibold">{title}</h1><p className="hidden text-xs text-slate-400 sm:block">PSPCL electricity meter operations</p></div><div className="relative"><button onClick={()=>setOpen(v=>!v)} className="flex items-center gap-3 rounded-lg px-2 py-1.5 hover:bg-slate-50"><div className="flex h-8 w-8 items-center justify-center rounded-full bg-brand-100 text-brand-700"><UserIcon className="h-4 w-4"/></div><div className="hidden text-left sm:block"><p className="text-sm font-medium">{profile?.name||user?.name}</p><p className="text-xs capitalize text-slate-400">{profile?.role||user?.role}</p></div></button>{open&&<><div className="fixed inset-0 z-10" onClick={()=>setOpen(false)}/><div className="absolute right-0 z-20 mt-2 w-56 rounded-xl border bg-white py-2 shadow-panel"><div className="border-b px-4 py-3"><p className="text-sm font-medium">{profile?.email||user?.email}</p></div><button onClick={async()=>{await logout();nav('/login',{replace:true})}} className="flex w-full items-center gap-2 px-4 py-3 text-sm text-rose-600 hover:bg-rose-50"><LogOutIcon className="h-4 w-4"/>Sign out</button></div></>}</div></header>}
